---
description: Operate the current repository under PCM v1
---

Activate and follow the existing `pcm-v1` skill for this request.

Load the skill before proceeding. Treat `docs/PCM.md` as the semantic authority and the `pcm-v1` skill as the operational layer. Do not redefine, duplicate, or replace either one.